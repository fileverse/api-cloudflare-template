{
  "name": "api-cloudflare-template",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "One-click Cloudflare deploy template for Fileverse API (PostgreSQL)",
  "scripts": {
    "start": "node index.js",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  },
  "dependencies": {
    "@fileverse/api": "^0.0.5"
  },
  "devDependencies": {
    "@anthropic-ai/cloudflare-containers": "^0.0.2",
    "@cloudflare/workers-types": "^4.20250214.0",
    "wrangler": "^4.25.0",
    "typescript": "~5.5.0"
  }
}
